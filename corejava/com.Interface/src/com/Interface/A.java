package com.Interface;

public interface A {
   void lion();  //in interface class we can't write public abstract by default it is created
              //and we can't assign private and protected accessmodifiers in methods
              //and we can't create object
  abstract public void dog();
  abstract public void cat();
  abstract public void parrot();
  abstract public void rat();
  
}
